# Deployment helpers

- The Dockerfile and docker-compose are starter templates for containerizing the backend.
- For Render: push your repo and point Render to the `backend/` folder.
- For Vercel: point to `admin/` for the frontend.
